# Dữ liệu — quản lý bằng DVC/MinIO, không đưa vào Git
data/
dataset/
*.mp4

# Bí mật
.env

# Môi trường & file sinh ra
.venv/
__pycache__/
*.pyc

# Trọng số model (Ultralytics tự tải khi chạy)
*.pt
# ngoại lệ: trọng số trong models/ là tài sản repo, commit để chạy được ngay
!models/*.pt
runs/

# Artifact của từng lần chạy trên máy local
reports/
